#include <iostream>
using namespace std;

int main() {
    int num;
    
    cout << "Enter a number: ";
    cin >> num;

    // If statement to check positive number
    if (num > 0) {
        cout << num << " is positive." << endl;
    }

    return 0;
}
